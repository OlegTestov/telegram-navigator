"""Gemini prompts for post classification."""

CLASSIFICATION_PROMPT = """Ты классификатор постов Telegram-канала.

Для каждого поста определи:
1. topics — список из 1-3 тем (на русском языке). Выбирай из существующих тем если подходят. Создавай новую тему только если ни одна существующая не подходит.
2. description — яркий заголовок до 50 символов. Максимум смысла, минимум воды. НЕ начинай с "Гайд по", "Обзор", "Инструкция по", "Руководство по". Пиши как газетный заголовок — цепляюще и по делу. Примеры хороших: "Красивые диаграммы в Excalidraw на автомате", "Claude Code: перемотка и управление контекстом", "5 принципов разработки в эпоху ИИ".
3. usefulness — оценка от 1 до 10, насколько пост полезен для читателей (10 = очень полезный, практичный; 1 = малополезный, проходной).

Существующие темы:
{existing_topics}

Правила:
- Темы должны быть общими (минимум 3 поста должны подходить).
- Название темы: 1-3 слова, существительное или именное словосочетание.
- Описание: короткое, ёмкое, без клише. СТРОГО до 50 символов.
- Если пост не содержит полезного контента (реклама, приветствие, опрос без содержания), тема "Прочее".

Ответ строго в JSON:
[
  {{
    "post_index": 0,
    "topics": ["Продуктивность", "Привычки"],
    "description": "Метод 1-3-5 для планирования дня",
    "usefulness": 8
  }}
]

Посты для классификации:
{posts}"""

TOPIC_SUMMARY_PROMPT = """Напиши краткое саммари (2-3 предложения) для темы "{topic_name}" на основе описаний постов этой темы.

Описания постов:
{post_descriptions}

Саммари должно давать общее представление о чём эта тема в канале. Не перечисляй посты, а обобщи."""

TOC_GROUPING_PROMPT = """Ты организатор контента Telegram-канала. Тебе даны посты с описаниями, ID и тегами (в квадратных скобках).

Задача: раздели ВСЕ посты на ровно {groups_count} непересекающихся тематических групп.

Правила:
- Каждый пост должен попасть ровно в ОДНУ группу.
- Группы должны быть примерно равного размера (±30%).
- Название группы: 2-4 слова, ёмко отражающие содержание.
- Группы не должны пересекаться по смыслу.
- Посты с общими тегами скорее всего связаны — старайся держать их в одной группе. Например, если несколько постов помечены [Разработка, Инструменты] — это серия, не разбивай её.
- Посты без содержательной ценности (реклама, приветствия) отнеси в наименее подходящую группу, не создавай для них отдельную.

Ответ строго в JSON:
[
  {{
    "group_name": "Название группы",
    "post_ids": [74, 102, 81, ...]
  }},
  ...
]

Посты:
{posts}"""
